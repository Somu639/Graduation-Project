# ---------------------------------------------------------------------------
# Spotify Discovery Analyzer - FULL ML stack (Python 3.11-3.13)
# Use in Docker / local for all features: LangChain RAG, ChromaDB/Pinecone,
# local embeddings, LLM agents, transformers, and scrapers.
#
# Install:  pip install -r requirements-full.txt
# NOTE: requires Python <3.14 (chromadb/torch/pinecone wheels). Use 3.12.
# ---------------------------------------------------------------------------

# Base deployable set
-r requirements.txt

# --- LangChain / RAG / agent orchestration ---
langchain==0.3.14
langchain-core==0.3.29
langchain-community==0.3.14
langchain-openai==0.2.14
langchain-anthropic==0.3.1
langchain-huggingface==0.1.2
langgraph==0.2.62

# --- LLM providers ---
openai==1.59.6
anthropic==0.42.0

# --- Vector stores ---
chromadb==0.5.23
langchain-chroma==0.1.4
# Pinecone (optional cloud backend)
pinecone==5.4.2
langchain-pinecone==0.2.0

# --- Embeddings ---
sentence-transformers==3.3.1
langchain-voyageai==0.1.4
voyageai==0.3.2

# --- Scraping ---
praw==7.8.1
google-play-scraper==1.2.7
lxml==5.3.0
# NOTE: app-store-scraper is intentionally omitted here: v0.3.5 hard-pins
# requests==2.23.0 which conflicts with the rest of the stack. Install it in an
# isolated environment if you need the Apple App Store scraper:
#     pip install app-store-scraper

# --- Text processing / NLP ---
scikit-learn==1.6.0
transformers==4.47.1
nltk==3.9.1

# --- Data ---
numpy==2.2.1
